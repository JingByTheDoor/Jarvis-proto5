# Progress

## 2026-03-11

- what changed: Bootstrapped the Phase 0A workspace, added the canonical docs, defined the schema registry, shared enums, approval hashing, canonical-path policy, network-scope policy, untrusted-content guard, redaction pipeline, capability-token contract types, IPC contract map, and the full contract-level test suite for this slice.
- why it changed: The repo was greenfield, and the master sheet requires a frozen safety spine before shell, runtime, or UI work can safely proceed.
- files touched: `package.json`, `package-lock.json`, `tsconfig.json`, `vitest.config.ts`, `src/index.ts`, `src/shared/*`, `src/core/schemas/*`, `src/core/approval/hashes.ts`, `src/core/capabilities/*`, `src/core/planning/untrusted-content.ts`, `src/core/redaction/redactor.ts`, `tests/**/*`, `docs/constitution.md`, `docs/task_plan.md`, `docs/progress.md`, `docs/findings.md`
- what was tested: `npm run typecheck`; `npm test` with schema contract tests, IPC boundary tests, approval signature tests, redaction tests, renderer hardening config tests, canonical path tests, network scope / SSRF tests, and untrusted-content boundary tests.
- current status: Phase 0A is complete and verified. Phase 0 remains open because encrypted-at-rest implementation and verification are still deferred.
- next step: Implement the platform-backed encrypted-at-rest layer and its verification tests so Phase 0 can close before Phase 1 begins.

## 2026-03-11 Phase 0 Completion

- what changed: Added the Windows DPAPI-backed encrypted-at-rest provider, exported the new persistence contract surface, expanded prompt-injection coverage across all named untrusted source classes, and completed the constitution updates required to freeze persistence, quota, renderer-hardening, and crash-cleanup policy.
- why it changed: The master sheet does not allow Phase 0 to close until encrypted-at-rest verification passes and the remaining named hardening expectations are explicit in the canonical docs.
- files touched: `src/core/persistence/encrypted-at-rest.ts`, `src/index.ts`, `tests/persistence/encrypted-at-rest.test.ts`, `tests/policies/prompt-injection-boundary.test.ts`, `docs/constitution.md`, `docs/task_plan.md`, `docs/progress.md`, `docs/findings.md`
- what was tested: `npm run typecheck`; `npm test` covering 52 passing tests across schema contracts, IPC boundaries, approval signature equality/inequality, redaction, renderer hardening config, canonical path enforcement, network scope / SSRF policy, prompt-injection boundaries, untrusted-content boundaries, and encrypted-at-rest verification for logs, memory stores, caches, sensitive settings, and export staging.
- current status: Phase 0 is complete and verified on Windows. The repo is ready to start Phase 1.
- next step: Build the thin Electron shell with a hardened preload bridge and the minimum Command Center frame required by Phase 1.

## 2026-03-11 Phase 1 Completion

- what changed: Added the Electron main-process shell, `app://jarvis` protocol registration, default-deny session and navigation guards, the narrow typed preload bridge, the React/Vite dashboard frame, the Command Center skeleton, and scaffold pages for Tasks & Projects, Second Brain, Connections, and Settings. Also split the desktop API contract into a renderer-safe shared module so the browser bundle does not depend on preload/runtime code.
- why it changed: The Phase 1 sheet requires a real hardened shell before any risky capability can be exposed, and it requires the primary UI surfaces to exist where the first golden workflow will later land.
- files touched: `package.json`, `package-lock.json`, `tsconfig.json`, `vitest.config.ts`, `vite.config.ts`, `index.html`, `src/main/*`, `src/preload/*`, `src/renderer/*`, `src/shared/desktop-api.ts`, `src/shared/index.ts`, `src/shared/ipc.ts`, `tests/main/*`, `tests/preload/*`, `tests/renderer/*`, `docs/task_plan.md`, `docs/progress.md`, `docs/findings.md`
- what was tested: `npm run typecheck`; `npm test` with 61 passing tests; `npm run build`; `npm run smoke:electron`. The new coverage includes sender validation, default-deny permission/navigation/popup behavior, preload schema validation, renderer shell smoke tests, and a real Electron startup smoke pass.
- current status: Phase 1 is complete and verified. The app boots, the shell is hardened, the preload bridge is typed, and the default UI path is in place without exposing risky capability.
- next step: Start Phase 2 by wiring task intake, typed repo/file tools, compile-time manifest generation, and exact diff preview for the first real repo/edit workflow.

## 2026-03-11 Phase 2 Completion

- what changed: Added the real Phase 2 preview compiler, compile guards for unknown args/side effects/scope rejection, the first typed repo/file tool registry, exact diff preview generation, main-process task preview handling, and the Command Center inspection surfaces for route explanation, plan summary, manifest summary, compiled actions, and typed edit diffs.
- why it changed: The master sheet requires the first golden repo/edit workflow to become real and inspectable before risk, approval, or execution are broadened.
- files touched: `src/core/compile/*`, `src/core/planning/task-routing.ts`, `src/core/tools/*`, `src/main/ipc.ts`, `src/renderer/app.tsx`, `src/renderer/desktop-api.ts`, `src/renderer/pages/command-center-page.tsx`, `src/renderer/styles/app.css`, `tests/compile/*`, `tests/tools/*`, `tests/renderer/app-shell.test.tsx`, `tests/fixtures.ts`, `docs/task_plan.md`, `docs/progress.md`, `docs/findings.md`
- what was tested: `npm run typecheck`; `npm test` with 72 passing tests; `npm run build`; `npm run smoke:electron`. The new coverage includes compile/manifest validation, rejection of unknown args / unknown side effects / unknown scope, typed-tool precedence, exact diff preview generation, typed git/file tool behavior, and dashboard inspection of route, plan, manifest, and diff surfaces.
- current status: Phase 2 is complete and verified. The first repo inspection and safe edit preview workflow is real, local, deterministic, and compiled into typed manifest actions.
- next step: Start Phase 3 by adding deterministic risk classification, effect preview generation, and approval semantics on top of the compiled manifest path.

## 2026-03-11 Phase 3 Slice A

- what changed: Added a pure simulation layer that generates `EFFECT_PREVIEW` records, classifies deterministic risk from compiled actions plus previews, derives exact approval scope for risky actions, upgrades the preview response to stop at `simulation_ready` or `awaiting_approval`, and surfaces simulation summary plus approval scope in the Command Center.
- why it changed: Phase 3 requires risk and simulation to become inspectable before approval or execution can broaden, and the first golden workflow needed its pre-approval effect model to be real instead of implied.
- files touched: `src/core/simulation/*`, `src/core/compile/task-preview.ts`, `src/shared/ipc.ts`, `src/main/ipc.ts`, `src/renderer/app.tsx`, `src/renderer/desktop-api.ts`, `src/renderer/pages/command-center-page.tsx`, `tests/simulation/*`, `tests/compile/task-preview.test.ts`, `tests/renderer/app-shell.test.tsx`, `tests/fixtures.ts`, `tests/persistence/encrypted-at-rest.test.ts`, `docs/task_plan.md`, `docs/progress.md`, `docs/findings.md`
- what was tested: `npm run typecheck`; `npm test` with 77 passing tests; `npm run build`; `npm run smoke:electron`. The new coverage includes deterministic risk classification, effect-preview generation, raw-shell risk-floor defaults, simulation confidence downgrade, hidden-widening regression checks, complexity-vs-risk distinction, and renderer inspection of simulation and approval scope.
- current status: Phase 3 is in progress. The risk/simulation slice is verified, but approval submission still stops at schema validation and the remaining escalation/session-approval semantics are not closed.
- next step: Implement approval decision handling against compiled manifests and finish the remaining Phase 3 widening/escalation tests before moving on to execution.

## 2026-03-11 Phase 3 Completion

- what changed: Added an in-memory approval registry that binds approval requests to compiled manifests, upgraded approval IPC to a typed request/response path, required `execution_hash` in approval payloads, wired the Command Center to submit deny/approve decisions and show receipts, and added approval-registry plus main-IPC integration tests for exact-match and hidden-widening behavior.
- why it changed: Phase 3 could not close while approval submission was still validation-only; the master sheet requires exact approval semantics, explicit rejection of silent widening, and inspectable approval state before execution opens.
- files touched: `src/core/approval/approval-registry.ts`, `src/core/schemas/contracts.ts`, `src/shared/ipc.ts`, `src/shared/desktop-api.ts`, `src/preload/bridge.ts`, `src/main/ipc.ts`, `src/renderer/app.tsx`, `src/renderer/desktop-api.ts`, `src/renderer/pages/command-center-page.tsx`, `src/renderer/styles/app.css`, `src/index.ts`, `tests/approval/*`, `tests/main/ipc.test.ts`, `tests/preload/bridge.test.ts`, `tests/ipc/ipc-boundary.test.ts`, `tests/renderer/app-shell.test.tsx`, `tests/fixtures.ts`, `tests/simulation/simulation-engine.test.ts`, `docs/constitution.md`, `docs/task_plan.md`, `docs/progress.md`, `docs/findings.md`
- what was tested: `npm run typecheck`; `npm test` with 84 passing tests; `npm run build`; `npm run smoke:electron`. The new coverage includes approval-decision recording against compiled manifests, exact approve-once acceptance, hidden-widening rejection on signature/hash drift, never-session-approvable enforcement, session-approval exact-match reuse checks, preload approval-response validation, and renderer approval receipt rendering.
- current status: Phase 3 is complete and verified. The app now reaches deterministic risk, simulation, and exact approval semantics without opening execution early.
- next step: Start Phase 4 by building the privileged execution runtime, capability-token gating, structured live events, run-log persistence, and narrow v1 attestation.

## 2026-03-11 Phase 4 Completion

- what changed: Added the privileged manifest-execution runtime, the in-memory capability-token store, encrypted run-log persistence, typed file-tool attestation, run execution/history IPC surfaces, live `RUN_EVENT` publishing from the main process, and the Command Center / Tasks & Projects review surfaces for results, artifacts, attestation, and persisted run history. Also added the persistence-failure fallback so execution results remain visible without falsely claiming `review_ready`.
- why it changed: Phase 4 required the first golden workflow to execute only approved compiled actions, stream live events, persist reviewable logs, and attest what actually ran before the project could broaden into guarded shell and memory search.
- files touched: `src/core/approval/approval-registry.ts`, `src/core/attestation/typed-tool-attestation.ts`, `src/core/capabilities/capability-token-store.ts`, `src/core/compile/task-preview.ts`, `src/core/events/run-log-store.ts`, `src/core/execution/manifest-executor.ts`, `src/main/index.ts`, `src/main/ipc.ts`, `src/preload/bridge.ts`, `src/shared/desktop-api.ts`, `src/shared/ipc.ts`, `src/index.ts`, `src/renderer/app.tsx`, `src/renderer/desktop-api.ts`, `src/renderer/pages/command-center-page.tsx`, `src/renderer/pages/tasks-projects-page.tsx`, `src/renderer/styles/app.css`, `tests/capabilities/*`, `tests/attestation/*`, `tests/events/*`, `tests/execution/*`, `tests/ipc/ipc-boundary.test.ts`, `tests/main/ipc.test.ts`, `tests/preload/bridge.test.ts`, `tests/renderer/app-shell.test.tsx`, `tests/fixtures.ts`, `docs/constitution.md`, `docs/task_plan.md`, `docs/progress.md`, `docs/findings.md`
- what was tested: `npm run typecheck`; `npm test`; `npm run build`; `npm run smoke:electron`. Coverage now includes compile/simulate/attest consistency, capability-token replay and parallel double-submit, crash cleanup / revocation, run-log persistence, exact file attestation, attestation deviation, structured event-stream rendering, and persistence-failure fallback behavior.
- current status: Phase 4 is complete and verified. The first golden workflow now reaches `EXECUTE -> ATTEST -> REVIEW` through approval-gated typed execution with visible results and persisted review state.
- next step: Start Phase 5 by adding guarded shell as an audited escape hatch and minimal local recall/search for resume-previous-task flow.

## 2026-03-12 Phase 5 Completion

- what changed: Added the guarded-shell escape hatch with typed-tool precedence, compile/simulate/approval/execution support, structured shell receipts, local recall search across run history and operator notes, resume-previous-task entry points, and the matching UI/IPC surfaces for guarded commands and searchable local recall.
- why it changed: The master sheet requires a constrained raw-shell escape hatch plus useful local recall before routing, memory, or optional systems broaden, and both had to remain local-first, approval-gated, provenance-labeled, and redacted.
- files touched: `src/core/tools/guarded-shell-tool.ts`, `src/core/tools/tool-registry.ts`, `src/core/memory/basic-recall.ts`, `src/core/planning/task-routing.ts`, `src/core/compile/*`, `src/core/simulation/*`, `src/core/attestation/typed-tool-attestation.ts`, `src/core/execution/manifest-executor.ts`, `src/main/ipc.ts`, `src/preload/bridge.ts`, `src/shared/*`, `src/renderer/*`, `src/index.ts`, `tests/planning/*`, `tests/tools/guarded-shell-tool.test.ts`, `tests/memory/*`, `tests/compile/task-preview.test.ts`, `tests/attestation/typed-tool-attestation.test.ts`, `tests/preload/bridge.test.ts`, `tests/ipc/ipc-boundary.test.ts`, `tests/renderer/app-shell.test.tsx`, `tests/support/temp-repo.ts`, `docs/constitution.md`, `docs/task_plan.md`, `docs/progress.md`, `docs/findings.md`
- what was tested: `npm run typecheck`; `npm test` with 106 passing tests across 27 files; `npm run build`; `npm run smoke:electron`. New and updated coverage includes raw-shell classification and precedence, structured shell receipts, guarded-shell attestation, search provenance, recall redaction, resume-task flow, renderer guarded-shell/recall surfaces, and the full existing workflow gate.
- current status: Phase 5 is complete and verified. Guarded shell is constrained, classified, approval-gated, and attested; local recall improves continuity without weakening trust or secret-handling rules.
- next step: Start the Phase 6 proof gate by measuring golden-workflow stability, latency, steps/clicks, and resume usefulness before broadening routing, durable memory, or optional systems.

## 2026-03-12 Phase 6 Slice A

- what changed: Added typed workflow-proof contracts, an encrypted local workflow-proof store, proof-record and proof-summary IPC, renderer-side proof recording for preview/approval/execute/resume milestones, and a proof-gate dashboard in Settings backed by local summary data.
- why it changed: Phase 6 cannot broaden routing or optional systems until the current golden workflow is proven stable and low-friction, so the smallest correct next step was to measure the proof gate locally before any broader expansion.
- files touched: `src/core/schemas/*`, `src/core/proof/workflow-proof-store.ts`, `src/shared/*`, `src/main/ipc.ts`, `src/preload/bridge.ts`, `src/renderer/app.tsx`, `src/renderer/pages/settings-page.tsx`, `src/renderer/pages/tasks-projects-page.tsx`, `src/renderer/pages/command-center-page.tsx`, `src/renderer/desktop-api.ts`, `src/index.ts`, `tests/contracts/schemas.test.ts`, `tests/proof/workflow-proof-store.test.ts`, `tests/main/ipc.test.ts`, `tests/preload/bridge.test.ts`, `tests/ipc/ipc-boundary.test.ts`, `tests/renderer/app-shell.test.tsx`, `tests/fixtures.ts`, `docs/constitution.md`, `docs/task_plan.md`, `docs/progress.md`, `docs/findings.md`
- what was tested: `npm run typecheck`; targeted Vitest coverage for proof-store persistence/summary, main IPC proof handlers, preload proof IPC validation, IPC boundary envelopes, schema registry additions, and renderer proof-gate surfaces.
- current status: Phase 6 is in progress. The proof gate now exists locally and is measurable, but advanced routing, durable memory, optional providers, challenger logic, and quota expansion remain deferred behind that proof gate.
- next step: Use the recorded proof data to validate the current golden workflow over real local journeys, then only broaden into advanced routing or optional systems once the evidence is sufficient.
